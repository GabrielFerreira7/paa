#pragma once
#include <vector>
#include <algorithm>
#include <iterator>
#include <math.h>
using namespace std;

float distance(vector<float> v1, vector<float> v2);
float BruteForceClosestPair(vector<vector<float> > vec);